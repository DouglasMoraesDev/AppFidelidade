## Exemplo de variáveis de ambiente para produção (Renomeie para .env ou configure no Railway)
# String de conexão MySQL (substitua pela URL fornecida pelo Railway)
DATABASE_URL="mysql://root:xBupgaNtJLhdplUfgqQxslAfyNxcROSu@shuttle.proxy.rlwy.net:18002/railway"

# Porta que o servidor deve escutar (opcional)
PORT=4000

# Se quiser que o backend sirva o frontend build automaticamente
SERVE_FRONTEND=true
