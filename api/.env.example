# Exemplo de variáveis de ambiente para produção
# Renomeie este arquivo para .env ou configure as variáveis diretamente no Railway/Render/Heroku

# String de conexão MySQL fornecida pela plataforma (Railway, Vercel, etc.)
# Formato: mysql://usuario:senha@host:porta/database
DATABASE_URL="mysql://root:xBupgaNtJLhdplUfgqQxslAfyNxcROSu@shuttle.proxy.rlwy.net:18002/railway"

# Porta que o servidor Express vai escutar (padrão: 4000)
PORT=4000

# Host que o servidor vai bind (padrão: 0.0.0.0 para aceitar conexões de fora)
HOST=0.0.0.0

# JWT Secret para autenticação (substitua com um valor forte em produção)
JWT_SECRET="Voyageturbo13."

# Se true, o backend serve o frontend compilado (dist/). Deixar sempre true em produção.
SERVE_FRONTEND=true

# (Opcional) Se quiser usar um build que não sea do repo, pode deixar vazio
# NODE_ENV é setado automaticamente para "production" no Dockerfile
